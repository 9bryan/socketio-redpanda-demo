# socketio-redpanda-demo
Quick demonstration of using javascript to produce data to flask-socketio and put it to rest in kafka/redpanda

1. Install python packages `pip install -r requirements.txt`
1. Make env file `cp example.env .env` and update values `vi .env`
1. Serve app `python app.py`

TODO:
1. Containerize
